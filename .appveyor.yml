image: Visual Studio 2015

init:
  - git config --global core.autocrlf input

clone_folder: c:\app
shallow_clone: true

matrix:
  fast_finish: false

platform:
- x64

configuration:
- Debug
- Release

install:
- git submodule update --init --recursive

build_script:
- cd src
- cmake .
- mak3
